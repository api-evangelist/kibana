naftiko: 1.0.0-alpha2
info:
  label: Kibana APIs — APM server schema
  description: 'Kibana APIs — APM server schema. 1 operations. Lead operation: Save APM server schema. Self-contained Naftiko
    capability covering one Kibana business surface.'
  tags:
  - Kibana
  - APM server schema
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    KIBANA_API_KEY: KIBANA_API_KEY
capability:
  consumes:
  - type: http
    namespace: kibana-apm-server-schema
    baseUri: https://{kibana_url}
    description: Kibana APIs — APM server schema business capability. Self-contained, no shared references.
    resources:
    - name: api-apm-fleet-apm_server_schema
      path: /api/apm/fleet/apm_server_schema
      operations:
      - name: saveapmserverschema
        method: POST
        description: Save APM server schema
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: apikey
      key: Authorization
      value: '{{env.KIBANA_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: kibana-apm-server-schema-rest
    port: 8080
    description: REST adapter for Kibana APIs — APM server schema. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/api/apm/fleet/apm-server-schema
      name: api-apm-fleet-apm-server-schema
      description: REST surface for api-apm-fleet-apm_server_schema.
      operations:
      - method: POST
        name: saveapmserverschema
        description: Save APM server schema
        call: kibana-apm-server-schema.saveapmserverschema
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: kibana-apm-server-schema-mcp
    port: 9090
    transport: http
    description: MCP adapter for Kibana APIs — APM server schema. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: save-apm-server-schema
      description: Save APM server schema
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: kibana-apm-server-schema.saveapmserverschema
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
